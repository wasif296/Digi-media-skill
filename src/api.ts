// export interface ProjectData {
//   _id?: string;
//   title: string;
//   category: string;
//   result: string;
//   imageUrl: string;
// }

// export const getProjects = () => api.get<ProjectData[]>("/portfolio");
// export const addProject = (data: ProjectData) => api.post("/portfolio", data);
// export const updateProject = (id: string, data: ProjectData) => api.put(`/portfolio/${id}`, data);
// export const deleteProject = (id: string) => api.delete(`/portfolio/${id}`);